macro include {-> <stdio.h> <-}
macro include {-> <stdlib.h> <-}

def print(fmt: [&u8])
  spawn {-> 
    printf("printed: %s\n", fmt);
  <-}
end

our msg: [&u8] = spawn our {->"My name is Lucas!";<-}
pendence print
arg [!fmt] msg
call

spawn our {->
  free(msg);
  return 0;
<-}